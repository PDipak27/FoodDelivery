app:
  jwt:
    secret: test-secret-key-which-is-long-enough-for-hs256-algorithm-padding
    access-token-expiry-ms: 900000
    refresh-token-expiry-days: 7

spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: auth
